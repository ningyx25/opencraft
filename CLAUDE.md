# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

OpenCraft — a Fabric mod for Minecraft 1.21.11. Started from the FabricMC example-mod template and renamed to its own identity: mod id `opencraft`, base package `com.swaydy.opencraft`, display name "OpenCraft".

The mod adds an **AI game assistant** that accompanies the player: a bound companion entity that follows the player and can be chatted with via `/opencraft ask <message>`, with replies generated by any OpenAI-compatible Chat Completions API (async, off the server thread). Also includes the original `ai_logo_block` decorative/functional block.

## Build & Run Commands

Uses the Fabric Loom Gradle plugin — specifically the `net.fabricmc.fabric-loom-remap` variant, v1.17-SNAPSHOT — via the Gradle wrapper (Gradle 9.5.1). Java 21 is the compile target; a JDK 21+ toolchain is required (CI builds with JDK 25).

| Task | Command |
|---|---|
| Full build (compile + jar + remap) | `./gradlew build` |
| Launch Minecraft client with the mod | `./gradlew runClient` |
| Launch dedicated server with the mod | `./gradlew runServer` |
| Run headless Fabric gametests | `./gradlew runGametestServer` |
| Run tests | `./gradlew test` |
| Compile only | `./gradlew compileJava compileClientJava` |
| Clean | `./gradlew clean` |

- No linter (Checkstyle/Spotless/etc.) is configured.
- Gametests live in `src/main/java/com/swaydy/opencraft/test/` (registered under the `fabric-gametest` entrypoint) and run via the `gametestServer` Loom run config (`-Dfabric-api.gametest=true`; filter defaults to `opencraft:*`, override with `./gradlew runGametestServer -PgametestFilter=opencraft:<name>`). The main test (`assistantLifecycleAndChat`) summons the assistant for a mock player, triggers a real LLM HTTP request (config `ai.baseUrl` must point at a reachable OpenAI-compatible endpoint, e.g. a local mock), verifies history, and dismisses. **注意**：gametest 服务器在测试期间会“冲刺”tick（远快于 20tps），而流式回复在独立线程上按墙钟时间到达——因此“等回复/等动作”的断言用 `thenWaitUntil(() -> { if (未就绪) throw new GameTestAssertException(Component.literal(...), (int) helper.getTick()); })` 轮询，而不是固定 `thenIdle(N)`（1.21.11 的 `thenWaitUntil(Runnable)` 靠抛 `GameTestAssertException` 保持轮询；抛其它异常会直接崩服）。
- `./gradlew test` runs the pure-Java JUnit tests in `src/test/java/` (currently `LlmClientTest` — new LlmClient 的 SSE chunk 协议/错误映射/看门狗/退化路径单测，和 `AgentLoopGuardsTest` — 守卫与重试策略，均对本地 mock 运行，无 Minecraft 运行时）。JUnit 5 deps 声明在 `build.gradle`（含 `junit-platform-launcher`，Gradle 9.5.1 需要）。
- Build output jars go to `build/libs/`. CI (`.github/workflows/build.yml`) runs `./gradlew build` on push/PR and uploads `build/libs/`.

## Architecture

### Split source sets — common vs client
Loom's `splitEnvironmentSourceSets()` (in `build.gradle`) separates code into two source sets so client-only classes never ship in the dedicated-server jar:
- **Common** — `src/main/java/`, package `com.swaydy.opencraft`. Runs on both client and dedicated server. Entrypoint: `com.swaydy.opencraft.OpenCraftMod` implements `ModInitializer` (`onInitialize`).
- **Client** — `src/client/java/`, package `com.swaydy.opencraft.client`. Client-only. Entrypoint: `com.swaydy.opencraft.client.OpenCraftModClient` implements `ClientModInitializer` (`onInitializeClient`). Rendering, keybindings, client-side networking, and client mixins belong here.

Both entrypoints are registered in `src/main/resources/fabric.mod.json` under `entrypoints.main` and `entrypoints.client`.

### Mixins
Two separate mixin configs, each with `compatibilityLevel: JAVA_21` and `injectors.defaultRequire: 1`:
- **Common mixins** — `src/main/resources/opencraft.mixins.json`, package `com.swaydy.opencraft.mixin`, listed under the `mixins` array (currently `OpenCraftMixin`, a no-op `@Inject` into `MinecraftServer.loadLevel`). Add new server/common mixin classes here.
- **Client mixins** — `src/client/resources/opencraft.client.mixins.json`, package `com.swaydy.opencraft.client.mixin`, listed under the `client` array (currently `OpenCraftClientMixin`, a no-op `@Inject` into `MinecraftClient.run`), and gated to `environment: "client"` in `fabric.mod.json`. Add new client mixin classes here.

When adding a mixin class, you must also register its name in the corresponding JSON's array — an unregistered mixin will not apply. With `defaultRequire: 1`, a mixin whose target method is missing fails hard at load, so verify target method names against the current mappings before relying on one.

### Namespaced identifiers
Route every `Identifier` through the helper in `src/main/java/com/swaydy/opencraft/OpenCraftMod.java`:
```java
public static final String MOD_ID = "opencraft";
public static Identifier id(String path) {
    return Identifier.fromNamespaceAndPath(MOD_ID, path);
}
```
`MOD_ID` is the single source of truth for the namespace (the SLF4J logger is named after it too). Do not hand-build `Identifier`s with a different namespace.

### Mappings
Uses `loom.officialMojangMappings()` (official Mojang mappings, not Yarn) — set in `build.gradle`. Refer to existing source files for the exact class/import names in use (e.g. the entrypoint imports `net.minecraft.resources.Identifier`).

## AI Assistant Module (common code)

- `entity/AiAssistantEntity.java` — `PathfinderMob` subclass. Owner is stored in `SynchedEntityData` as `Optional<EntityReference<LivingEntity>>` (1.21.11 removed UUID serializers/`getPersistentData`; `EntityReference` is the new pattern). `registerGoals()` runs from `Mob.<init>` BEFORE instance fields initialize — never reference fields there; create goals inline. Overrides `addAdditionalSaveData`/`readAdditionalSaveData` (`ValueOutput`/`ValueInput`, not `CompoundTag`) to persist the owner (the `Following` flag was removed with the follow modes). Calls `setPersistenceRequired()` in the constructor.
- `entity/ModEntities.java` — `EntityType` registration via `EntityType.Builder.of(...).build(ResourceKey)`, `FabricDefaultAttributeRegistry.register`, and multi-assistant helpers `findAssistantsFor(ServerPlayer)` (list of a player's assistants), `findNearestAssistantFor(ServerPlayer)` (nearest by bound-block distance — used by ask/dismiss/reset targeting), `findAssistantsBySelector(ServerPlayer, String)` (match by bare name / `名字 (x,y,z)` / `名字(x,y,z)` — used by `ask <名字> <消息>` to pick a specific assistant), `findAssistantsBoundTo`/`findAssistantBoundTo`/`isConfigBlockBound` (block → assistant). No spawn egg (removed: the assistant must bind an AI Logo Block, so a spawn egg only creates a meaningless unbound assistant that is auto-cleared).
- `ai/LlmClient.java` — 与 OpenAI 兼容 Chat Completions 接口通信的客户端，基于官方 **openai-java**（`com.openai:openai-java:4.52.0`，`/home/swaydy/openai-java/openai-java-example` 有使用样例），设计参考 deepseek-harness 的 **dsh-llm** 包；无 Minecraft 依赖，可对 mock 服务器单测。**提供者无关词汇**：内容为类型化块 `Block`（`TextBlock`/`ReasoningBlock`/`ToolCallBlock`/`ToolResultBlock`），`Message(role, blocks)` 只有 `USER`/`ASSISTANT`（system 走 `Request.system`），`Request` 承载 baseUrl/apiKey/model/system/messages/tools/temperature/maxTokens/stop/timeoutSeconds，工具为结构化 `ToolSchema`（`fromJson` 从 OpenAI tools JSON 解析）。**流式 chunk 协议**（`stream(Request, ChunkSink)`）：`BlockStart`/`TextDelta`/`ReasoningDelta`/`ToolCallDelta`/`BlockEnd`/`Usage`/终端 `Finish` 按序回调在发起线程；工具参数保持**原始分片字符串**（`BlockEnd(ToolCallBlock)` 携带组装好的完整块，调用方负责组装——参考 dsh BlockAssembler 的分工）；每个调用恰好一个终端 `Finish`（成功或失败都不抛出）。**稳定错误码**：失败统一为 `LlmFailure(message, code, status, providerRetryAfterMs, requestId)`，code 为可路由稳定串（`Codes`：AUTH/RATE_LIMIT/QUOTA/CONTEXT_WINDOW_EXCEEDED/SERVER/TIMEOUT/TRANSPORT/EMPTY_RESPONSE/MALFORMED_RESPONSE/STALLED…），HTTP 状态 + 措辞 → code 的纯函数 `httpErrorCode`（移植 dsh；含上下文超限/配额措辞分类）。**重试分离**：`chat(Request)`/`stream(...)` 每次只尝试一次，`maxRetries(0)`，重试由 `LlmRetryPolicy` 按 code 路由（`retryableCode`）。**SDK 适配要点**：懒加载共享基客户端 + 每次请求 `withOptions` 覆盖 baseUrl/apiKey/timeout（`warmUp()` 预热，OpenCraftMod 启动时调用）；空 API Key 用占位 `"opencraft"`（SDK 鉴权闸门要求非空，本地无鉴权端点忽略）、非法字符 key → INVALID_CREDENTIAL（`checkApiKey`）；`reasoning_content`（思维链）经 SDK 各模型 `_additionalProperties()` 双向透传（流式从 delta、非流式从 message 捕捉，请求侧 `putAdditionalProperty("reasoning_content", ...)` 原样回传）；工具 schema 经 `ObjectMappers.jsonMapper().readValue(json, ChatCompletionFunctionTool.class)` 反序列化（任意 JSON Schema 走 FunctionParameters additionalProperties 保留）。**SSE 读取看门狗**：SDK 的 OkHttp readTimeout 只保证“两次读之间间隔”，服务端拿连接后不吐数据仍可能无限阻塞——`stream()` 保留守护看门狗线程，按 **idle** 计时（每收到一个 chunk 重置，`max(5, timeoutSeconds)+2s` 无数据即 `close()` 底层流），统一报 `STALLED`（LlmRetryPolicy 判为**不可重试**，快速失败并释放忙锁；dsh 把 idle 超时归 TIMEOUT，本 mod 单独成码保留“卡死不重试”决策）。**兼容性——“端点忽略 stream”退化路径**：SDK 的 SSE 解析器要求 `data:` 行，端点直接返回完整 JSON 时解析出 0 个 chunk，适配层对该请求**再发一次非流式** `chat()`（幂等端点返回同一回复），合成 chunk 序列（`BlockStart/TextDelta/BlockEnd` + `Finish`）。纯 Java 单测 `src/test/java/com/swaydy/opencraft/ai/LlmClientTest.java`（本地 mock：chat/SSE chunk 协议/错误映射/看门狗/退化路径/checkApiKey）。
- **插件系统 + Agent 预设（`agent/` 包，common 源码集）**——彻底替换旧 `[ACTION: ...]` 标记系统（`ai/AiAction`/`ai/AiActionParser` 已删除）。参考 deepseek-harness「万物皆插件」：`AssistantPlugin`（贡献 tools / systemPromptFragment / gameContextFragment / registerGoals）、`ToolDefinition`（name + description + JSON Schema + executor）、`ToolContext`（server/assistant/owner/level）、`ToolResult`、`AgentDefinition`（插件组合 + persona + maxToolRounds）、`AgentRegistry`（静态注册表，`OpenCraftMod.onInitialize` 调 `init()`）、`AgentRuntime`（agentic loop：LLM 原生 function calling → 服务端执行工具 → 喂回结果 → 循环到最终文本回复）。插件在 `plugins/`：`AssistantControlPlugin`（teleport_to_player，双形态通用）、`PlayerActionsPlugin`（玩家 bot 的“真玩家动作”：player_goto/player_stop/player_jump/player_look/player_find/player_mine/player_place/player_craft/player_inventory/player_item_move/player_hotbar_select/player_hand_to_player，**全部按原版真实玩家的操作链路**：`player_mine` 走真实客户端-服务端挖掘流程——`PlayerMovementController.startMining` 发 `START_DESTROY_BLOCK`、服务器 `ServerPlayerGameMode` 按工具速度推进破坏进度、等 `1/getDestroyProgress` tick 后发 `STOP_DESTROY_BLOCK`（校验 ≥0.7 后破坏并按工具掉落）——挖掘耗时与真实玩家完全一致（木镐挖石头 23 tick；过慢 >30s 直接拒绝让模型换工具）；`player_place` 用 `ServerPlayerGameMode.useItemOn`（可选 `sneak=true` 走原版 shift 分支——跳过方块交互直接放置，才能对着箱子/熔炉放方块而不是打开它们）；`player_craft` 用真实配方书流程（随身 `inventoryMenu` 2×2 或工作台 `CraftingMenu` 3×3 → `handlePlacement` 自动摆料 → 循环 shift 点结果 `quickMoveStack` → `removed` 退回余料）；`player_item_move` 在助手自己的 `InventoryMenu` 里三次真实点击交换（护甲部位校验/穿戴回调全原版），丢弃走原版 `Player.drop`；`player_hotbar_select` 走 `setSelectedSlot`（与原版换手键服务端处理一致）。装备/主手变更的客户端同步全部由 bot tick 链的 `detectEquipmentUpdates` 自动完成（插件不再手动广播）。**挖掘/移动解耦**：`stop()` 只停移动不中断挖掘（跟随每 tick 的 stop 不再误杀挖掘），`player_stop` 才连挖掘一起 `cancelMining()`；**movement.tick 每 tick 把薄片着地判定写回 `setOnGround`**——bot 纯水平 move() 不刷新该标志，而原版 `Player.getDigSpeed` 对 `!onGround` 有 ÷5 挖掘减速（不写回则木镐挖石头 23→113 tick，踩过此坑）；**`player_find`** 按关键词/ID 找方块或实体并返回【精确坐标+方位+距离+可达性】（条目与助手垂直差 ≥3 格时附 `(N blocks below/above you)`；列出的方块全部在脚下 ≥5 格时附”underground——走过去只到其正上方，需向下挖”提示——修复”模型带着地下 9 格的石头坐标 goto、到了头顶却挖不到”的认知缺口；**掉落物可按物品名搜**——`isEntityQuery` 对能解析成物品的词也走实体查询，`matchesEntity` 的 ItemEntity 分支匹配手持物品短名/描述键/解析 ID，结果标签 `Dropped <物品名>`——修复”挖出的圆石掉在地上、模型按名搜索却只命中方块，无法定位掉落物”）（`target` 支持完整/短/描述名或中文关键词如 “log/石头/箱子/玩家”，方块经 `AiCompanionService.resolveBlock` 精确解析或注册表子串展开、实体按关键词/实体类型查询），模型据此判断“东西在哪”而不是拿着无坐标计数瞎猜——砍树会先 `player_find oak_log` 再精准挖；`player_look` 的实体条目也带坐标+方位。旧存档遗留的实体形态插件（Movement/Perception/Mining/Inventory/Crafting/Combat）已删除——它们从未被任何预设装配，实体形态助手（仅旧存档兼容）现在是纯聊天伴侣。预设 `presets/`：`chat_agent`（纯聊天）、`general_agent`（默认；玩家式行动：移动/挖掘/放置/合成/递物/观察 + 传送到主人身边，maxToolRounds=8）。**预设只决定 LLM 行为，绝不决定身体形态**——不存在也不再存在 `player_agent` 预设。**跟随模式（自动）**：助手默认跟随主人（同维度走近、太远/跨维度瞬移回主人身边，见玩家形态条目）；玩家下达指令（`/opencraft ask` 或聊天，即 `AgentRuntime.runAsync` 成功获取忙锁）后**退出跟随**专注执行，指令完成（最终回复/报错/中断）后**回到跟随**。没有 `set_mode` 工具、潜行右键切换、互动界面跟随按钮或可配置的跟随/停止/瞬移距离——跟随完全自动（距离用代码内常量 3/2/24 格）。
  - **内置技能模块（`agent/skills/` + `resources/skills/`，skill-creator 方法论）**——SKILL.md 规范的结构化经验注入：`Skill`（frontmatter `name`/`description`/`requires_tools` + Markdown 正文的纯 Java 解析器，name 须小写 kebab-case 且与目录名一致，坏文件返回 null 跳过）+ `SkillLibrary`（按 `skills/index.json` 索引从 classpath 加载 jar 内置技能，懒加载单例，索引缺失/单条损坏只告警不抛——技能是增强不是依赖）；`Prompts.system` 把适用技能渲染成 `# Skills` 大节（每个技能 `## name` + description + 正文）拼在 Capabilities 之后、Game Context 之前。**绑定优先，双重过滤**：技能由预设点名（`AgentDefinition.skills` 组件，旧五参构造器兼容为空）才会注入——未绑定的技能不占上下文，管理粒度是"哪个预设加载哪些技能"；绑定了还要过 `requires_tools`（当前预设的可用工具全命中）——纯聊天预设即使绑了挖掘技能也收不到。`AgentRegistry.init` 一次性校验各预设绑定的技能名真实存在（拼写错误告警，不会静默失效）。游戏 Agent 无法按需读文件，progressive disclosure 退化为"按绑定+工具相关性整节注入"。**内置技能清单（general_agent 绑定 5 个）**：`dig-down-staircase`（阶梯下沉挖法——两挖一走循环、1×1 竖井反模式、卡井恢复）、`gather-wood`（砍树：find→自下而上挖树干、可达优先/换树优先于搭柱、按 [Event] picked-up 计数）、`craft-toolchain`（开局工具链：原木→木板→木棍→工作台→木镐→石镐，按 craft 的精确报错驱动——"requires a crafting table"→摆工作台重试、"not enough materials"→按缺补采、挖掘层级门槛）、`mine-and-collect`（定量采集：find 的 dy/地下标注→配阶梯下沉、先查工具层级、按 picked-up 而非 broken 计数）、`regroup-with-owner`（回到主人：走路 vs teleport_to_player 的取舍 + 距离缰绳——mine 目标限距主人、goto 目标限距助手自身，goto 报错文案已修正为与实际校验一致）。技能文案直接引用工具的真实报错与事件，让模型遇到信号即知下一步。新增内置技能 = 建 `resources/skills/<name>/SKILL.md` + 登记进 `index.json` + 在预设的 skills 列表里绑定。单测 `SkillTest`/`SkillLibraryTest`（遍历 index.json 通用校验：每条必加载、目录名=name、requires_tools 为已知工具）/`AgentDefinitionSkillsTest`。
  - **Agentic loop 的成熟设计（参考 deepseek-harness）**——`AgentRuntime` 的循环已按 dsh 的成熟 Agent 模式强化（类注释详述；均有 JUnit 单测 `src/test/java/com/swaydy/opencraft/agent/AgentLoopGuardsTest.java`，`./gradlew test` 运行）：
    - **LLM 请求重试**（`agent/LlmRetryPolicy.java`，参考 dsh-llm-retry）：限流（HTTP 429）/5xx/超时/连接/IO 等瞬时失败，在**未吐出任何字符前**按指数退避 + 抖动（500ms→10s、±10%，最多 2 次）重试同一轮请求，重试期间“正忙”锁保持持有；空响应（无内容也无工具调用）视为可重试的退化完成（EMPTY_RESPONSE）。已吐出部分文本后失败不重试（避免重复上屏）。**SSE 读取看门狗超时（`request-stalled`）判为不可重试**——服务端拿到连接后长时间不吐数据，重试只会再白等，直接失败让玩家重问/中断。
    - **重复工具调用守卫**（`agent/RepeatToolGuard.java`，参考 dsh-repeat-tool-reminder）：跟踪同一次提问内「连续、完全相同（工具名 + 参数 JSON 深度按键排序后一致）」的调用，第 3 次注入温和提醒、第 5/8 次注入点名工具/次数/参数的详细提醒（作为 user 消息追在 tool 结果后），打断模型“笨笨地重复同一动作”的死循环；换工具/换参数即重置链条；参数 JSON 无法解析时按原文兜底。提醒文案**不再建议“结束任务”**（旧文案会把等待异步动作的模型劝停，任务半途而废），改为“等待异步动作别重复下达/换做法/如实汇报受阻”。**goto 轮询豁免**（`agent/MovementQueries.java` 纯 Java 可单测）：与在途手动移动同目标（容差 1 格，含 (x+0.5,z+0.5) 方块中心换算与数字字符串）的重复 `player_goto` 是“等待到达的再确认”而非死循环——不执行、不进守卫，改回信息型结果（“已在途中，走路需要几秒，利用等待轮做别的”）。
    - **停滞守卫**（`agent/StallGuard.java`，参考 sandbox/轮次空转防护）：连续 ≥3 轮只调用纯观察工具（`player_look`/`look_around`/`player_inventory`/`inspect_block`）而世界/背包无变化时，注入一次 user 提醒让模型“二选一：给出结论结束任务，或执行真实动作（先 `player_find` 拿坐标再动手）”——打断慢/弱 API 下“卡在某一步”的纯观察空转；继续空转由 maxToolRounds 总结兜底。有 JUnit 覆盖。
    - **工具结果标记 + 裁剪**（`agent/ToolResultPruner.java`，参考 dsh-compaction-tool-result-pruner）：tool 结果统一以 `[工具名 成功/失败]` 开头（模型先读标记再读内容）；超过 1200 字符的结果保头 900 / 尾 200、中间省略，防上下文无限膨胀。参数 JSON 无法解析时把原文回显给模型让其自纠。
    - **每轮工具调用上限**（`MAX_TOOLS_PER_ROUND = 6`，参考 dsh-agent-loop 的 maxParallelToolCalls）：单轮超过 6 个调用时多余的直接返回“已达上限，先观察结果再继续”。
    - **历史压缩**（参考 dsh-compaction-basic）：历史长度超过 `maxHistoryMessages×2` 时，先在工作线程用一次非工具 LLM 调用（复用当前模型/persona，temperature 压低到≤0.5）把最旧区段压缩成 `<compacted-summary>` 记忆摘要并保留最近 `maxHistoryMessages` 条原文，再开始本轮；摘要未变短（< 原区段一半）或请求失败时自动退回直接裁剪；旧摘要会被后续压缩自然并入新摘要。压缩请求在 worker 线程执行、历史的落地替换在服务端线程（避免并发写）。
    - **向玩家提问（暂停/恢复）**（参考 dsh-tool-ask-user）：模型在指令含糊或行动有破坏性/不可逆影响时调用核心工具 `ask_player`——循环暂停（“正忙”锁保持持有）、把问题呈现给玩家（命令模式 `AiCompanionService.speakAsAssistant` 广播；GUI 模式发 "reply" 事件到窗口）；玩家用 `/opencraft answer <回答>` 回答后（`AgentRuntime.answer`，按绑定方块键控 + 仅主人有权限）把回答以 user 消息写回并恢复下一轮；超时 `ASK_TIMEOUT_MS=90s` 未答则自动按合理假设继续并在回复中说明。`PENDING_ASKS`（`Map<GlobalPos, PendingAsk>`）原子移除保证 answer 与超时只有一个能恢复；同批其他工具调用在提问时不执行（先问不做）。核心工具 schema 由 `AgentRuntime.coreToolSchemas()` 在每轮请求附加（不在 `toolMap()` 里，故不影响 `toolMap().size()` 相关测试）。
    - **中断交互**（`AgentRuntime.interrupt(GlobalPos)`，入口：`/opencraft interrupt`（== `/opencraft stop`）命令 + 配置界面聊天页「中断」按钮（C2S `AiConfigInterruptPayload`，按绑定方块坐标路由））：卡住时可**立即中止当前任务并重新提问**。实现：`LIVE`（活动 LoopContext 按方块键控）取到 ctx → 置 `ctx.cancelled=true` → 清 `PENDING_ASKS` 与 `PENDING_ACTIONS`（超时任务稍后取不到条目自动 no-op；先 `clearActionCallback()` 关动作事件通道再 `stop()`，不触发恢复）→ 玩家形态 `movement().stop()` → **立即释放忙锁**（RUNNING/LIVE）→ 回传“已中断”（GUI "reply" 事件/命令系统消息）+ `finishOverlay` 清浮层 + 历史补一条“（上一条任务被玩家中断…）”。**并发正确性**：所有异步回调入口（runRound/onDone 工具与最终路径/onError/scheduleRetry 续跑/continueAfterAnswer/maybeReveal）先 `if (ctx.cancelled) return;`——旧循环迟到的回调不广播、不续轮、不误删新 loop 正在持有的忙锁；in-flight 的阻塞 `HTTP.send` 无法被杀，但返回后进入上述 cancelled 守卫（且有了 SSE 读取看门狗，卡死请求也会自行超时）。另：`runAsync` 的“正忙”拒绝在 GUI 模式也推 `error` 事件（不再只在系统聊天可见）。有 gametest `agentInterruptReleasesLoop` 覆盖“中断后第二条提问不被拒绝并正常回复”。
    - **任务计划跟踪**（参考 dsh-tool-todo + system-prompt 注入）：核心工具 `task_plan` 让模型以 `{steps:[{content,status}]}` 整单替换维护结构化计划（`agent/TaskPlan.java`，纯 Java 可单测；status ∈ pending/in_progress/completed，content 非空不重复）；`AgentRuntime.runRound` **每轮都重建 system 文本**（`buildSystemWithPlan` → `Request.system`，仍是单条 system 开头）——有任务计划时把「【当前任务计划】…」注入，同时也让游戏上下文每轮刷新（坐标/天气/装备不是提问时的静态快照）——多步任务不丢进度、状态不过期。`task_plan` 不参与重复调用守卫。
    - **延迟动作结果（goto/mine/place 的事件续轮）**——修复“任务半途而废”的治本机制（参照 ask_player 的暂停/恢复）：这三个工具只启动动作并返回 `ToolResult.deferred`（`ToolResult` 增第三分量 `deferred`；同步完成的路径——如 place 就近直接放——仍返回普通 ok/error），`AgentRuntime.executeTools` 在批内全部工具执行完后注册 `PENDING_ACTIONS`（`Map<GlobalPos, PendingAction>`，含 nextRound/目标/挖掘的背包快照）并暂停循环（`ctx.pausedByAction`，“正忙”锁保持持有）；`PlayerMovementController` 新增**动作事件回调**（`setActionCallback`/`completeAction`/`clearActionCallback` + 注册代次令牌防陈旧回调），在到达（含 Δy 垂直差与“卡住后被传送”标记）、`stop()` 被停止、挖掘完成（延迟 30 tick 上报——原版掉落物自带 10 tick 拾取保护期，事件太早背包差分永远显示"什么都没捡到"；差分为空时用 `ActionEvents.noPickupYetNote()`"尚未入包、下一轮查背包"口径，不说"可能没捡到"）、挖掘中止（离触及范围/方块消失）时触发 → `AgentRuntime.resumeWithEvent` 注入 `[Event] 工具名 finished/failed: 文案` user 消息并自动续轮（挖掘附背包差分 `ActionEvents.inventoryDiffText`：捡到/消耗了什么）；**超时兜底（goto/place 45s、mine 50s）以两参 `remove(key, pa)` 绑定动作实例**——旧动作的事件早已恢复、map 已换新动作时，陈旧超时任务必须 no-op，否则会误杀新动作并关掉它的事件通道（曾造成每分钟一次的伪超时连锁，模型被迫 teleport_to_player 自救）；一批只允许一个延迟动作（共用一个移动控制器），同批第二个返回错误；同批 `player_stop` 停掉动作时注册检查“不在途”立即以停止事件恢复，不空等超时；`interrupt`/LLM 报错/`finishLoop`/`shutdown` 清理 pending + 回调（报错路径顺带 `stop()+cancelMining()`，任务死了不留移动）。`teleport_to_player` 传送前停掉在途移动（否则传送后 bot 朝旧目标走回去）。事件文案集中在 `agent/ActionEvents.java`（纯 Java 可单测）。
    - **终止守卫**（`agent/TaskCompletionGuard.java` 纯 Java 可单测）——防“文本即终止”误杀未完成任务：模型输出纯文本（无工具调用）时，若 `task_plan` 有未完成步骤（`TaskPlan.hasUnfinished()`）或异步动作仍在途（手动移动/挖掘，`asyncActionInFlight`），暂缓收尾（最多 `MAX_HOLDS=2` 次，不与模型对抗）：文本作为**中间消息**广播到世界聊天（进度可见）但不写历史/不释放忙锁，注入 `[System] 任务未完成…继续用工具行动` 提醒续轮；总结轮与步数将尽的轮不适用。GUI 模式暂缓时不发最终 "reply" 事件（浮层照常淡出，下轮 "thinking" 接上）。
    - **状态可见性**：发起工具调用时提示“正在行动（第 N/M 步：工具名…）”（`command.opencraft.agent.executing` 现带 3 个占位符）。
  - **游戏内确认问答**：`/opencraft answer <回答>`（`command/ModCommands.java`）回答「最近的」助手（即提问的那个）的待回答提问；无待回答提问/非原提问者时报 `command.opencraft.answer.none`。lang 键：`command.opencraft.ask.question`（提问呈现）、`command.opencraft.answer.{blank,none,ok}`。chat_agent `maxToolRounds` 已 1→3（容纳一次澄清提问 + 恢复收尾）。
  - **mock LLM 注意**：`bin/mock_llm_server.py` 的 SSE 流式已在逐块输出后**去掉 `time.sleep`**——gametest 服务器“冲刺”tick，慢速流式（每块 5ms）会让并行测试的回复在 tick 预算内到不了位，导致“等回复”断言 flaky 超时。
- **助手形态抽象（`assistant/AiAssistant.java` 接口）**——统一抽象：`AiAssistantPlayer`（真 ServerPlayer 假玩家，`formId()=="player"`，**当前唯一形态**）与 `AiAssistantEntity`（PathfinderMob 底座，`formId()=="entity"`，仅旧存档遗留兼容）。Agentic loop、对话、历史、命令、界面只依赖接口的身体无关方法；身体专属能力由插件提供。`assistant/AssistantFacade.java` 跨形态统一路由：一个方块至多一个助手（跨形态判定占用），查找/召唤/送走/对话都按统一规则；**召唤一律走玩家形态**（`PlayerAssistantService.summonFor` → `PlayerList.placeNewPlayer` 正式进服），与方块配置的 agent 预设无关；旧存档遗留的实体助手在重新召唤时先被送走迁移。`agent/ToolContext.java` 携带 `AiAssistant`，提供 `assistantEntity()`/`assistantPlayer()` 便捷强转（实体插件只在实体形态运行，玩家插件只在玩家形态运行）。
- **玩家形态助手（`assistant/player/`，common 源码集）**——把 AI 助手做成“像多人联机客户端一样进服”的真玩家：`AiAssistantPlayer extends ServerPlayer`，拥有普通玩家的全部内容（可以不用但不能没有）：真正的 43 槽玩家背包（1.21.11：36 主背包 + `EntityEquipment` 7 装备槽含身体/坐骑鞍）、游戏模式、玩家式动作（`ServerPlayerGameMode.destroyBlock/useItemOn` 真实破坏/放置、掉落物自动拾取——玩家形态本身就是 Player）。核心机制：`FakeConnection extends Connection`（黑洞连接：`Connection(PacketFlow.SERVERBOUND)` 构造后 channel 为 null；`setupInboundProtocol/setupOutboundProtocol` 必须重写为 no-op 否则 placeNewPlayer 会 NPE；**`send/runOnceConnected/flushChannel` 必须显式丢弃**——父类实现在 `isConnected()==false` 时把每个包 `pendingActions.add(...)` 永久积压（channel 永远不会出现、队列永不被冲刷），bot 移动时区块/实体追踪包源源不断发来，不丢弃就是无界内存泄漏，修过一次的教训）；`PlayerAssistantService.summonFor` 用**确定性 UUID（按绑定方块）** 建 GameProfile → `PlayerList.placeNewPlayer(new FakeConnection(), player, CommonListenerCookie.createInitial(profile, false))` 正式进服（加入 PlayerList、实体追踪、Tab、广播，对其他人就是一个客户端玩家；**进服的系统玩家名固定为 `PlayerAssistantService.SYSTEM_NAME`（现为 "IAISwayDy"）**，与方块配置显示名解耦——想整体改 bot 进服名（加入消息/Tab/list）改这个常量即可，聊天显示名仍读配置；**多个 bot 同名是故意允许的**——原版本来就允许多个同名玩家（服务端只校验 UUID），mod 内部路由全靠配置显示名、存档靠确定性 UUID，重名只有 Tab/加入消息的观感重复、无功能问题，别当 bug “修复”成进服名唯一）→ 载入旧存档（`loadPlayerData` + `TagValueInput.create`）→ 生存模式 + `abilities.invulnerable`（不摔伤/不淹死/不饿死，`causeFallDamage` 覆写为 false）。**移动**：`ServerPlayer` 服务端不应用输入/重力（服务器信任客户端移动包），`PlayerMovementController` 每 tick 用 `move(MoverType.PLAYER, delta)` 带碰撞直接驱动位置 + 自施加重力/跳跃/卡住传送回退。**两个关键细节**：(1) 着地判定用脚底 2mm 薄切片与方块碰撞实时计算（`hasGroundBelow`），**不依赖 `Entity.onGround`**——bot 纯水平 move() 不刷新该标志，靠它门控重力会导致走出平台边缘后浮空；切片必须极薄，否则 bot 会悬停在离地几厘米处（切片碰地即判着地、重力被清零）；(2) 移动时用 `Mth.atan2` + 每 tick 限幅 15° 同步平滑转向 yRot/yHeadRot/yBodyRot（朝向移动方向，不侧滑）；(3) **跳跃**（“助手能跳”的关键）：重力块在着地时**只清零 `vy<=0`，保留上升速度**——旧版无条件 `vy=0` 会在下一 tick 吞掉跳跃初速度，“卡住反射跳”从未真正起跳（用户反馈“不能跳”的根因，已修复+gametest 覆盖）；`movement().jump()`（模型经 `player_jump` 工具触发）原地起跳；移动时前方是 1 格实心台阶（脚部整块 + 其上方空气）时 `climbableStepAhead` 触发**主动起跳**爬升（不等卡住）；2 格以上高墙不浪费跳，交给 60 tick 卡住传送回退。**跟随模式（自动）**：`AiAssistantPlayer` 有 `following` 字段（默认 true，不持久化），`AiAssistant.isFollowing/setFollowing` 为其接口方法（legacy 实体形态默认不跟随）。`PlayerAssistantService.keepSafeState` 每 tick：无敌/食物满 + 跟随逻辑（同维度：距主人 >24 格瞬移、>3 格且 `!movement().isManual()` 时 `moveTo(主人)`、≤2 格时 `stop()`）；`onSlowTick` 每 40 tick：安全网校验 + 跨维度跟随（主人换维度时瞬移跟随）。`AgentRuntime.runAsync` 成功获取忙锁后 `beginTask`（`setFollowing(false)` + 清 `pendingFollowResume` + `movement().stop()`），loop 收尾（最终回复/报错/中断）时 `finishLoop`/`endTask`（`setFollowing(true)`）——玩家下达指令期间助手不跟随，指令完成后自动回到跟随。**跟随恢复延迟到动作空闲**：`endTask` 时若手动移动/挖掘仍在途则置 `pendingFollowResume`（立即恢复会被跟随逻辑召回在途动作——“走一半掉头回家”），`keepSafeState` 每 tick 检查移动/挖掘都空闲后才真正 `setFollowing(true)`；`beginTask` 清除该标志（新指令作废旧任务的延迟恢复）。`PlayerMovementController` 在**到达目标时复位 `manual=false`**（手动移动完成后跟随才能接管）。`AiAssistantPlayer.tick()` 由 `ServerLevel` 实体循环驱动（`ServerPlayer.tick` 不调 super，所以重力/移动全部自管）；**与真实玩家一致的完整 tick 链**：真实玩家每 tick 被驱动两次——连接监听器（`Connection.tick → ServerGamePacketListenerImpl.tick → doTick()`，含 `Player.tick → LivingEntity.tick`：装备变更检测/广播 `ClientboundSetEquipmentPacket`、状态效果、食物等）+ 实体循环的 `ServerPlayer.tick()`；bot 的 FakeConnection 不在服务器连接列表里、监听器永远不 tick，因此 `AiAssistantPlayer.tick()` 开头手动补 `doTick()`（同样的顺序）——装备穿/脱、属性修饰器等行为与真实进服玩家无异；`keepSafeState` 同时维持 `noGravity`（doTick 引入了原版 LivingEntity.tick，关掉原版重力避免与 PlayerMovementController 双重驱动——垂直/水平移动仍完全由控制器自管）。持久化：`addAdditionalSaveData/readAdditionalSaveData` 存 `OpenCraftOwner`/`OpenCraftDim`/`OpenCraftX/Y/Z`，背包/装备由 ServerPlayer 原样持久化到 `players/<uuid>.dat`（送走/服务器停止时 `PlayerList.remove/saveAll` 自动落盘，重进/重召唤时 `loadPlayerData` 读回）。右键交互 `interact(Player, hand)` 与实体版同规则（绑主/开双面板背包界面/非主人拒绝；潜行右键与普通右键相同，均打开背包界面）。`bin/mock_llm_server.py` 是本地 OpenAI 兼容 mock LLM（供无头 gametest 的聊天测试）。
- `entity/AiAssistantEntity.java` — legacy entity-form assistant; no follow (follow/stay modes removed): it stays where summoned and is driven only by explicit tasks. `readAdditionalSaveData` re-asserts `setPersistenceRequired()` because the NBT read resets the flag after construction. **任务系统与背包（玩家式）**：36 格 `SimpleContainer inventory`（`INVENTORY_SIZE = 36`，与生存玩家主背包一致；NBT `list("Inventory", ItemStack.OPTIONAL_CODEC)` 持久化，旧 9 格存档自动兼容）+ `AssistantTask currentTask`（服务端字段，不持久化）；任务以高优先级 Goal 挂到 goalSelector（`TaskHostGoal` 常驻优先级 0 代理驱动 currentTask，任务活跃时压制散步）。**装备栏用 LivingEntity 原生 `equipment`**（头盔/胸甲/护腿/靴子/主手/副手，随存档自动持久化；vanilla 每 tick 的 `detectEquipmentUpdates` 让装备属性/护甲值生效）。`tick()` 每 5 tick 扫描脚下 ItemEntity **自动拾取**（`pickupNearbyItems`：跳过掉落保护期物品，`take`+`onItemPickup` 播放拾取动画，背包满留原地）；`dropEquipment` 死亡时掉落全部背包+装备（玩家式）；`autoSelectMiningTool(state)` 挖掘前自动把背包里最快的工具换到主手；`giveToInventory/countOf` 供插件使用。`registerGoals()` 只注册基础 Goal（Float/LookAt/RandomLook/Stroll），插件 Goal 在首次 tick 由 `ensureAgentGoals()` 按当前 Agent 预设懒注册（registerGoals 在字段初始化前调用，无法读配置；当前无插件注册 Goal）。
- `ai/AiCompanionService.java` — history and the same-tick summon cache are keyed by the assistant's bound block (`Map<GlobalPos, ...>`; one block = one assistant = one independent memory, which survives dismiss/re-summon of the same block), async LLM calls on a daemon executor, callbacks marshalled to the server thread via `server.executeIfPossible`. `summonFor` uses a `RECENT_SUMMONS` cache because `addFreshEntity` only makes entities visible to lookups on the NEXT tick (PersistentEntitySectionManager loadingInbox), and scans upward for a safe spawn spot. `ask()`/`askGui()` 委托 `AgentRuntime.runAsync`（agentic loop，见上）；本类保留：召唤/送走/reset、历史存取（`getHistory`/`appendHistory`/`historyJson`/`historySize`）、显示/广播辅助（`sendGuiEvent`/`finishStreamReply`/`finishGuiReply`/`speakAsAssistant`/`teleportAssistantToPlayer`/`buildGameContext`/`resolveItem`/`notifyInventoryGain`）、**世界内流式浮层**（`nextStreamSession`/`streamOverlay`/`finishOverlay` → S2C `AssistantStreamPayloads.AssistantStreamPayload`：sessionId 按玩家递增分配、**一次提问共用一个会话**（在 `AgentRuntime.runAsync` 分配后经 `LoopContext` 贯穿所有轮次，客户端只认最新会话，杜绝并发流在同一浮层互相覆盖串扰）；包带 `name`（助手配置名），浮层渲染成 `[名字] …`；客户端 `client/render/AssistantStreamOverlay` 在世界内 HUD 渲染多行换行文本+▍ 光标、完成后停留淡出），以及打招呼用的无工具 `streamPlain`（打字机 reveal + 上浮层内联）。**交互状态也上浮层**：`runAsync` 先推“正在思考…”，`AgentRuntime.showExecuting` 在每轮工具执行前推“正在行动（第 N/M 步…）”到浮层（GUI 窗口另有 thinking 事件、命令模式另有聊天状态行）——思考/行动/逐字回复集中在同一个浮层看；任务正常结束、报错、被打断时都 `finishOverlay` 清掉浮层（报错/中断用其文案替换“正在行动…”），避免失败后 HUD 残留“正在行动第 N 步”来迷惑新对话。**GUI 模式的最终回复现在也会广播到世界聊天**（`AgentRuntime.finishReply` GUI 分支同时调 `finishGuiReply` 回传窗口 + `finishStreamReply` 广播世界聊天），不再是纯私人会话。**消息结构约束**：`AgentRuntime.buildSystem` 把 人设+插件提示词+游戏上下文 合并成**单条 system 且在开头**（vLLM/Qwen 约束）；历史只存 user/assistant 最终文本（tool 往返不写入长期历史）。**环境上下文（已扩充）**：`buildGameContext(player)` 随每轮重建的 system 文本刷新（新鲜，非静态快照），除原有 名字/维度/坐标/时间/生命/饥饿/经验/模式/主手 外新增——群系+气候（基温高/中/低）+降水、天气（晴/雨/雷暴）、天空亮度（`getSkyDarken`）、水下/氧气/着火、状态效果（前 3 个，含等级+秒）、装备摘要（36 主背包之后的 装备/副手/身体/坐骑鞍 槽位）、朝向方位、脚下方块 + 视线命中的方块（10 格 `ClipContext` 射线，含距离）、16 格内怪物数。`AiCompanionService.environmentCapsule(level,pos,radius)` 是玩家与助手**共用**的紧凑环境摘要（`PlayerActionsPlugin` 的 `gameContextFragment` 与 `player_look` 也复用它），`facingName/shortName/equipmentSummary/effectsSummary/lookingAt` 为配套小工具。
- **右键 AI 助手打开双面板背包界面**（`inventory/AssistantInventoryMenu.java` + `inventory/ModMenuTypes.java` + `client/gui/AssistantInventoryScreen.java`，MenuType `opencraft:assistant_inventory`）：右键自己的助手（普通或潜行）→ 服务端 `openMenu(new SimpleMenuProvider(...))`（标题=助手显示名）打开双面板背包——**左半是助手“按 E”的背包、右半是玩家自己的**，两半都是完整的原版 E 背包（护甲 4 + 副手 + 2×2 合成 + 结果槽 + 主背包 3 行 + 快捷栏，每侧 46 格共 92 格，槽位次序同原版 `InventoryMenu`：结果/合成/护甲/背包/快捷栏）。**直接复用原版组件（非仿制）**：菜单继承原版 `AbstractCraftingMenu`（玩家侧合成网格就是基类的 `addCraftingGridSlots`/`addResultSlot`，仅 x 偏移 `RIGHT_PANEL_X=180`）；助手侧合成网格用同样的原版 `TransientCraftingContainer`+`ResultContainer`+`ResultSlot`，两套网格的配方匹配/消耗都走原版静态 `CraftingMenu.slotChangedCraftingGrid`（**已用 access widener 开放**，见 `opencraft.accesswidener`）；主背包+快捷栏用原版 `addStandardInventorySlots`（两侧各一次，y 从 84 起）；护甲/副手**双端对称、容器绑定**（原版 `InventoryMenu` 的做法，这是硬约束：客户端同步 `container_set_content → slot.set` 会直接调槽位的写方法，任何"绑实体装备 API"的槽位在客户端都会走进原版 `onEquipItem` 的 `(ServerLevel)` 强转→**右键助手闪退**——修过一次的教训）：玩家形态助手（真 ServerPlayer）与查看者玩家都用 `AssistantInventoryMenu.PlayerEquipmentSlot`（原版包私有 `ArmorSlot` 的复刻：部位校验 `isEquippableInSlot`、堆叠 1、`setByPlayer→onEquipItem` 仅服务端、空槽占位图标）绑各自 `Inventory` 的原版装备索引（护甲 39-i、副手 `SLOT_OFFHAND`=40）；旧存档实体形态（PathfinderMob，装备不是 Container）服务端用 `EquipmentSlotSlot` 实体适配器、客户端退化为占位容器槽（同步填充）。屏幕 `AssistantInventoryScreen` 画原版常量 `AbstractContainerScreen.INVENTORY_LOCATION`（`inventory.png` 整张 176×166）两遍（原版 `InventoryScreen.renderBg` 同款 blit）；人物模型**直接调用原版静态 `InventoryScreen.renderEntityInInventoryFollowsMouse`**——左边渲染助手实体（随鼠标转头）、右边渲染玩家自己；标签为原版 E 标题位（titleLabelX=97）：左=助手名、右=原版 `container.crafting`；`imageWidth=356, imageHeight=166`。客户端 `MenuScreens.register(...)` 由原版 S2C 打开包自动创建；助手实体引用经复用的 S2C `AssistantInteractPayload`（打开包之后按序送达，字段 entityId 用于左侧模型渲染）下发，并有**准星实体兜底**（`Minecraft.crosshairPickEntity`，右键前正对着的就是助手——不依赖网络包时序，ID 包迟到/丢失也能渲染左侧模型，助手身上装备/手持物随模型显示）。服务端构造传助手真实容器+实体（客户端 `assistant==null` 时装备槽退化为占位容器、由槽位同步填充）+ `BooleanSupplier`（`() -> !isRemoved()`）做 `stillValid`：助手被送走/死亡时菜单失效自动关闭。Shift+点击左右互转（只进对方主背包+快捷栏；结果槽走原版 `onTake` 消耗材料）。未绑定助手右键=绑定主人；非主人右键=拒绝。有 gametest `assistantInventoryMenuLayoutAndTransfer` 覆盖（布局/装备槽直通实体/原版合成出工作台/双向转移/stillValid）与 `assistantBotInventoryOpensEndToEnd`（真实玩家形态 bot：右键 openMenu 全链路不抛异常、bot 头盔/盾牌/镐子实时出现在左侧槽位、光标点击给 bot 穿/取胸甲、**客户端式菜单对称性冒烟**（工厂同款 2 参构造的所有槽位被同步式写入不抛异常、玩家侧装备槽写入落到 Inventory 装备索引——防"客户端槽位碰实体 API→闪退"回归）、**doTick 装备链行为验证**（戴头盔数 tick 后护甲值>0、取下后归零——装备修饰器只在 doTick→LivingEntity.tick 的检测里增删））。`client/gui/AiAssistantInteractScreen.java`（旧右键聊天/送走界面）现为**不可达遗留代码**（无任何地方 new 它），保留备用；聊天/送走功能走配置界面第 4 页聊天窗口与 `/opencraft ask`/`dismiss`。
- `command/ModCommands.java` — `/opencraft` tree (`ask <msg>` targets the nearest assistant; `ask <名字> <msg>` targets a specific assistant by name with Tab-completion (same-name assistants are disambiguated with `名字(坐标)`, unknown names fall back to the nearest assistant); `answer <reply>`; `interrupt` (中止最近助手的当前任务，卡住时立即重新提问); `summon` binds the nearest unbound block; `dismiss [all]`; `status` lists every assistant; `reset [all]`; `help`; no reload — config lives in blocks). Note: 1.21.11 replaced `hasPermission(int)` with `Commands.hasPermission(Commands.LEVEL_GAMEMASTERS)`.
- **AI 配置载体 = AI 徽标方块（无外部配置文件）** — 配置全部保存在 `block/AiLogoBlockEntity.java`（BlockEntity，`AiBlockConfig` 15 字段，NBT 随方块存档持久化；注册用 `FabricBlockEntityTypeBuilder`）。`block/AiLogoBlock.java` 实现 `EntityBlock`；普通右键 → `ai/AiConfigHandler.openFor(player, pos, dim)` 把该方块的配置以 `ai/AiConfigData`（Gson JSON）通过 `AiConfigPayloads.AiConfigDataPayload`（S2C，带 BlockPos+维度+`bound`/`boundByMe` 绑定状态）发给客户端，打开 `client/gui/AiConfigScreen`（1.21 TabNavigationBar 四页 UI）；“保存/召唤/送走/聊天”通过 `AiConfigSavePayload`/`AiConfigSummonPayload`/`AiConfigDismissPayload`/`AiConfigChatPayload`（C2S，带目标方块坐标）发回服务器，`AiConfigHandler.save/summonWithBlock/dismissWithBlock/chatWithBlock` 写回方块实体 / 绑定助手 / 送走助手 / 发起对话。**配置界面第 4 页是内置聊天窗口**（不用 `/opencraft ask` 也能对话）：聊天页打开时发 `AiConfigChatHistoryPayload` 拉取本方块助手的对话历史，服务器以 S2C `AiConfigChatEventPayload`（kind: history/thinking/delta/reply/error，Component 文本，带方块坐标路由）回传——`chatWithBlock` 优先聊本方块已绑定的自己的助手，未绑定则自动用本方块召唤一个，他人占用则拒绝（`command.opencraft.summon.block_occupied`）；聊天页在「等待回复/流式」期间显示「中断」按钮（C2S `AiConfigInterruptPayload` → `AiConfigHandler.interruptWithBlock` → `AgentRuntime.interrupt`），卡住时可立即中止并重新提问；`AiCompanionService.askGui(...)` 与命令行 `ask` 共享同一份按方块键控的历史与 agentic loop；GUI 模式的流式增量除回传窗口（window delta）外也上到世界内流式浮层，最终回复除窗口 "reply" 事件外也会**广播到世界聊天**（不再纯私人会话，见 AiCompanionService 条目）。**“AI 功能”开关与“用本方块召唤助手”已合并为配置界面底部同一个按钮**：未绑定助手 → 点击召唤（绑定本方块）；已绑定自己的助手 → 点击送走（不召唤）；已被他人绑定 → 禁用。因此 `AiBlockConfig` 不再有独立的 `aiEnabled` 字段（旧存档的 `AIEnabled` NBT 标签已废弃），`isUsable()` 只要求 baseUrl 非空。**Agent 预设**：`AiBlockConfig.agent`（NBT `Agent`，默认 `general_agent`）只决定助手的 LLM 行为；配置界面第 2 页的「允许动作」开关已替换为「Agent 预设」下拉（chat_agent / general_agent，保存时写回 `agent`；**预设不决定身体形态**——助手一律是玩家形态的假玩家 bot，旧存档残留的 `player_agent` 值在保存时被归一为 general_agent）；`AiConfigData` 增 `agent` 字段（`allowActions` 已移除）。默认配置：`baseUrl`/`model`/`apiKey` 的默认值解析优先级为 **JVM 参数（`-Dopencraft.baseUrl/model/apiKey`）> 运行时环境变量（`OPEN_CRAFT_BASE_URL/MODEL/API_KEY`）> 编译期烘焙进 jar 的值 > 代码内置回退**（`AiBlockConfig.defaultBaseUrl()/defaultModel()/defaultApiKey()`）。烘焙机制：`build.gradle` 的 `generateDefaultsResource` 任务在编译期读取项目根目录 `.env`，把三个值以 XOR 混淆字节（key "opencraft"）写入 `build/generated/resources/opencraft/defaults.dat` 打进 jar；`AiBlockConfig.loadBakedDefaults()` 运行时从 classpath 读取并解码——因此 **jar 放到任何环境（含游戏启动器/独立服务器）都自带 `.env` 的默认值，且 jar 内无明文**（仅防肉眼，不防反编译）。`.env` 同时被注入到 run 任务（`runClient/runServer/runGametestServer`）的进程环境。内置回退：`baseUrl="https://api.openai.com/v1"`、`model="gpt-4o"`、apiKey 为 XOR 混淆字节，`name="小智"`。
  - **助手绑定配置方块**：`AiAssistantEntity` 持久化 `GlobalPos configBlock`（NBT `storeNullable("ConfigBlock", GlobalPos.CODEC)`），`getConfig()` 实时读绑定方块；没有绑定则用静态默认值。`AiCompanionService.summonFor(player, explicitGlobalPos)` 显式指定方块，否则 `AiConfigHandler.findNearestConfigBlock(level, center, radius, unboundOnly)` 找最近的**未绑定**方块（按区块遍历）。只有 op（`PlayerList.isOp(new NameAndId(profile))`）可保存。
  - **多助手共存规则（一方块一助手）**：每个 AI 徽标方块最多绑定一个助手——`summonFor` 目标方块已被他人助手绑定时返回 null（拒绝），重复召唤同一方块则幂等返回原实例；`summonFor` 自动模式只找未绑定方块。`AiAssistantEntity.tick()` 每 40 tick 校验——`configBlock` 为空或绑定方块已消失时 `discard()`（约 2 秒内清除刷怪蛋/旧存档遗留的无绑定助手）；`AiLogoBlockEntity.preRemoveSideEffects` 在方块被破坏时立刻 `discard()` 所有绑定助手并清除该方块的对话记忆。`/opencraft ask <消息>`（不带名字）与 `dismiss/reset` 按“绑定方块距玩家最近”的助手路由；`/opencraft ask <名字> <消息>` 按名字精确指定和哪个助手对话（`ModEntities.findAssistantsBySelector`，Tab 可补全，同名用坐标消歧，未知名回退最近助手）；`dismiss [all]`/`reset [all]` 可批量。助手显示名为 `[配置名字 (x,y,z)]`（`AiAssistantEntity.getDisplayName()` 服务端实时读方块配置的 `name`，`entity.opencraft.ai_assistant.named` = "%s (%s)"），聊天时能区分是哪个助手；`ModCommands.status` 也按此格式列出。
  - **密钥安全**：API Key 的任何部分都不发送给客户端（`AiConfigData.apiKey` 恒为空串，仅 `apiKeySet` 布尔告知"已设置/未设置"）；客户端要换密钥须把 `apiKeyChanged` 置 true 并填新值（留空=清除），否则服务端保留原密钥。界面 API Key 输入框用掩码格式化（`FormattedCharSequence.forward("•"...)`）。
  - UI 要点：`renderTransparentBackground`（勿用 `renderBackground`，1.21.11 会 "Can only blur once per frame" 崩溃）；**但容器界面（`AbstractContainerScreen` 子类，如 `AssistantInventoryScreen`）覆写 `renderBackground` 时必须补调 `this.renderBg(...)`**——1.21.11 把容器纹理绘制放在了 `renderBackground` 里，漏了它整个背景就是透明的（双面板背包界面踩过此坑）；控件回调里不要直接增删控件，用 `rebuildRequested` 标志延迟到 `tick()` 重建（翻页用 `Screen.rebuildWidgets()`）。潜行右键保留发光开关。网络包注册在 `OpenCraftMod.onInitialize`；客户端在 `OpenCraftModClient` 注册 S2C 接收器。`ServerPlayNetworking.send` 对 mock 连接会失败，发送已 try/catch 包裹。
- **调试模式（`debug/DebugLog.java`）**——开发测试用：把 mod 业务日志写入 `<游戏目录>/logs/opencraft-debug.log`，格式 `[时间] [分类] 内容`。分类（**不含 API Key**）：`chat`（收发消息/回复）、`llm`（每轮请求/重试/失败/轮数上限）、`tool`（工具调用+参数+结果/任务计划/重复提醒/单轮上限截断/停滞提醒）、`ask`（向玩家提问/恢复）、`interrupt`（玩家中断任务）、`history`（压缩/裁剪/清空）、`task`（实体任务开始/完成/失败/取消）、`pickup`（拾取）、`movement`（玩家形态移动指令/到达/停止/跳跃/卡住传送）、`player_action`（玩家式破坏/放置）、`summon`（召唤/送走/拒绝原因/旧档载入/安全网清除/方块被拆）、`bind`/`teleport`/`config`/`save`（玩家存档读写）/`death`/`debug`。日志为**覆盖式**：每次开启（启动参数或 `/opencraft debug on`）清空旧日志，只保留本次会话；单次会话内超 5MB 自动重写。开关：启动参数 `-Dopencraft.debug=true` / 环境变量 `OPEN_CRAFT_DEBUG=true` 默认开启；游戏内 `/opencraft debug on|off|status`（op）动态切换。埋点用 SLF4J 风格 `{}` 占位符（DebugLog 内部转成 `String.format` 的 `%s`）。有 gametest `debugModeLogsToFile` 覆盖。
- Client: `client/render/AiAssistantRenderer.java` extends `HumanoidMobRenderer<AiAssistantEntity, HumanoidRenderState, HumanoidModel<HumanoidRenderState>>` (1.21.11 render-state system; register via `EntityRenderers.register`, which Fabric's transitive access wideners expose) — 自带主/副手物品握持与头戴物品渲染，另挂 `HumanoidArmorLayer`（`ArmorModelSet.bake(ModelLayers.PLAYER_ARMOR, ...)`）让装备栏护甲像玩家一样显示。Custom 64×64 humanoid texture at `assets/opencraft/textures/entity/ai_assistant.png`.

## Key Versions (pinned in gradle.properties)
- Minecraft: 1.21.11
- Fabric Loader: 0.19.3
- Fabric API: 0.141.4+1.21.11
- Loom: 1.17-SNAPSHOT
- Java: 21

## Conventions for New Code
- Common code → `com.swaydy.opencraft`; client-only code → `com.swaydy.opencraft.client`.
- Common mixins → `com.swaydy.opencraft.mixin` + register in `opencraft.mixins.json`; client mixins → `com.swaydy.opencraft.client.mixin` + register in `opencraft.client.mixins.json`.
- Always namespace IDs via `OpenCraftMod.id("path")`.
